menu "Ntfs-3g"

config NTFS
	package "Ntfs-3g tools"
	build_select FUSE

if NTFS

config NTFS32
       bool
       default FUSE32

config NTFS_SRCDIR
       srcdir "Source directory"
       default "ntfs-3g_*"

endif

endmenu
