menu "File"

config FILE
	package "The file utility"

if FILE

config FILE_SRCDIR
       srcdir "Source directory"
       default "file-*"

config FILE_ZLIB
       bool "Enable zlib compression"
       build_select ZLIB

endif

endmenu
